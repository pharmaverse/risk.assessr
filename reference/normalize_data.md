# Normalize Nested Package Data

Flattens nested package metadata into a flat list for further
processing.

## Usage

``` r
normalize_data(results)
```

## Arguments

- results:

  The nested package metadata (list structure as produced by your
  package).

## Value

A list with flattened package information.
