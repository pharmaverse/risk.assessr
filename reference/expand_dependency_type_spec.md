# Expand Dependency Type Specification

Expands a dependency type specification into a character vector of
dependency types.

## Usage

``` r
expand_dependency_type_spec(x)
```

## Arguments

- x:

  A character string specifying the dependency type ("strong", "most",
  "all", or custom).

## Value

A character vector of dependency types.
