# Generate Coverage Section

Generates the Coverage section for the HTML report. For single-framework
packages returns a data frame; for multi-framework packages returns a
named list of data frames (one per framework).

## Usage

``` r
generate_coverage_section(assessment_results, pkg_name)
```

## Arguments

- assessment_results:

  \- input data

- pkg_name:

  \- name of the package
