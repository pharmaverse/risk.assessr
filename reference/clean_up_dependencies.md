# Clean Up Dependencies

Cleans up a list of dependency strings by extracting unique package
names.

## Usage

``` r
clean_up_dependencies(x)
```

## Arguments

- x:

  A character vector of dependency strings.

## Value

A character vector of unique package names.
